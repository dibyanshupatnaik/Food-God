"""
Python backend package for the nutrition planning application.

The FastAPI app and its helpers live in this directory so that running
`uvicorn backend.app:app --reload` works without extra PYTHONPATH tweaks.
"""

